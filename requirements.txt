# CPU-only PyTorch (saves ~1GB vs full CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# PDF extraction (uses torch for ML models)
docling>=2.3.0

# OCR fallback (lightweight, ONNX-based — no PaddlePaddle needed)
opencv-python-headless>=4.8.0
rapidocr-onnxruntime>=1.3.0

# File watcher
watchdog>=4.0.0

# Fuzzy string matching
rapidfuzz>=3.6.0

# Gemini LLM
google-genai>=1.0.0

# Web scraping for lookup agent
requests>=2.31.0
beautifulsoup4>=4.12.0

# Streamlit UI
streamlit>=1.30.0
pandas>=2.0.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
